"""Dataset layout constants (must match preprocess_dataset / global_features)."""

# Base global cond width: 3 log + michel + 3 extras (reco muon flag, γγ inv. mass, n charged prongs)
GLOBAL_COND_BASE_DIM = 7
